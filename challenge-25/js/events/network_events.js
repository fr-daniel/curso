(function( win, cod ){
    'use strict';

    /*
        nagivator.onLine : true ou false
    */
    
    if( navigator.onLine )
        alert( 'Você está online!' );
    else
        alert( 'Você está offline!' );
    

})( window, document );
