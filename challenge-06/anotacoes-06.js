/*
    Operador vírgula;
    var a, b = 2, c;

    function myFunction(){
        var x = 0;
        return (x += 1, x);
        // x recebe um e a função retorna x
    }

    Estrutura Condicional Switch;
    function myFunction(x){
        switch(x){
            case 1:
                console.log('x é 1');
                break;
            case 2:
                console.log('x é 2');
                break;
            default:
                console.log('x não é nem 1 nem 2);
        }
    }

    Estrutura de Repetição (Loop) While
    var counter = 0;

    while(counter < 10){
        console.log(counter);
        counter++;
    }

    var counter = 10;

    while( counter ){
        console.log( counter-- );
    }

*/